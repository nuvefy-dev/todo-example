# PostgreSQL (usado pelo docker-compose e pela API)
POSTGRES_USER=todo
POSTGRES_PASSWORD=todo
POSTGRES_DB=todo
POSTGRES_PORT=5432

# API NestJS — copie também para api/.env
# PORT=3001
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USER=todo
# DATABASE_PASSWORD=todo
# DATABASE_NAME=todo
# CORS_ORIGIN=http://localhost:3000

# Nuxt — copie também para web/.env
# NUXT_PUBLIC_API_BASE=http://localhost:3001
